export interface RegisterDto {
    /**
     * Username of a user
     */
    name: string;
    /**
     * Email of a user
     */
    email: string;
    /**
     * Password of a user
     */
    password: string;
}